version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: littlelabor-db
    environment:
      POSTGRES_USER: littlelabor
      POSTGRES_PASSWORD: littlelabor_dev_password
      POSTGRES_DB: littlelabor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U littlelabor"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
